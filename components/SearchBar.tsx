import { Search } from 'lucide-react';
import React from 'react';

const SearchBar = () => {
  return (
    <div>
      <Search className="size-5 hover:text-shop-light-green hoverEffect" />
    </div>
  );
};

export default SearchBar;
